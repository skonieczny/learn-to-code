angular.module("templates", []).run(["$templateCache", function($templateCache) {$templateCache.put("components/game.html","<h3>{{ game.name }}</h3>\n<ul>\n    <li ng-repeat=\"program in game.programsByScore()\">\n    	<a href=\"#/program/{{ program.id }}\">{{ program.name }}</a>\n    	by\n    	<a href=\"#/user/{{ program.author().id }}\">{{ program.author().name }}</a>\n    	Scored: {{ program.score }}\n    </li>\n</ul>\n<div expandable=\"Add program\" ng-controller=\"ProgramCreateForm\">\n	<h4>Create program</h4>\n	<form>\n		<input type=\"text\" ng-model=\"program.name\">\n		<input type=\"hidden\" ng-model=\"program.game\" value=\"{{ game.id }}\">	\n		<button ng-click=\"create()\">Create</button>\n	</form>	\n</div>\n");
$templateCache.put("components/program.html","<h3>{{ program.name }}</h3>\nby\n<a href=\"#/user/{{ program.author().id }}\">{{ program.author().name }}</a>\nReady: {{ program.ready }}\n<ul>\n    <li ng-repeat=\"match in program.matchesFromNewest()\">\n    	Match on {{ match.variant }}\n    	<a href=\"#/program/{{ match.program2.id }}\">{{ match.program2.name }}</a>\n    	by\n    	<a href=\"#/user/{{ match.program2.author().id }}\">{{ match.program2.author().name }}</a>\n    	against\n    	<a href=\"#/program/{{ match.program2.id }}\">{{ match.program2.name }}</a>\n    	by\n    	<a href=\"#/user/{{ match.program2.author().id }}\">{{ match.program2.author().name }}</a>\n    	Result: {{ match.score1 }}:{{ match.score2 }}\n    </li>\n</ul>\n");
$templateCache.put("main/main.html","<h3>List of Awesome Things</h3>\n<ul>\n    <li ng-repeat=\"game in games\">\n	    <a href=\"#/game/{{ game.id }}\">{{ game.name }}</a>\n    </li>\n</ul>\n");}]);